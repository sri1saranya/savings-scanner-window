import { React } from "react";
import { Scene, Router } from 'react-native-router-flux';

const RouterComponent = () => {
    return (
            <Router>
                <Scene key='root'>//parent scene

                </Scene>
            </Router>
    );
};
export default RouterComponent;